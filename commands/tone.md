---
description: Design and generate a Helix Stadium preset from a tone description
argument-hint: "<the tone, e.g. 'lead in Zombie by The Cranberries on my EC-1000'>"
---

Use the `tone` skill to author this tone:

$ARGUMENTS

Follow the skill's workflow end to end — research the reference, pick the
chain, wire snapshots and controllers, generate the `.hsp` into the tone
library, write its description, and report back in the skill's order.

With no arguments, ask what tone they want before doing anything else — never
invent a tone request.
